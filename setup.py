from setuptools import setup

setup(
    name='cloudify-ntp-plugin',
    version='1.0',
    author='lex',
    packages=['cloudify-ntp-plugin'],
    install_requires=['cloudify-plugins-common>=3.3'],
)
